@model TimesheetSystem.ViewModels.TimesheetNavigationViewModel
@* week selector: simpler than a calendar control for moving around weeks; would not work well for long timescales*@
<div class="form-group mb-4">
	<div class="d-flex align-items-center gap-2">
		<label class="form-label mb-0 fw-bold" style="white-space: nowrap;">Week Starting:</label>
		<div class="input-group flex-grow-1">
			<input type="text" class="form-control" value="@Model.WeekStartDate.ToString("dddd, dd MMMM yyyy")" readonly />
			<a asp-controller="Timesheet"
			   asp-action="Index"
			   asp-route-userId="@Model.SelectedUserId"
			   asp-route-weekStart="@Model.WeekStartDate.AddDays(-7).ToString("yyyy-MM-dd")"
			   class="btn btn-secondary">Prev. week</a>
			<a asp-controller="Timesheet"
			   asp-action="Index"
			   asp-route-userId="@Model.SelectedUserId"
			   asp-route-weekStart="@Model.WeekStartDate.AddDays(7).ToString("yyyy-MM-dd")"
			   class="btn btn-secondary">Next week</a>
		   @* could add a datepicker here: select date, jump to that date's week, needs some script *@
@* 		<form asp-controller="Timesheet" asp-action="Index" method="get" class="d-inline">
			<input type="hidden" name="userId" value="@Model.SelectedUserId" />
			<input type="date"
				   class="form-control"
				   style="max-width: 40px;" />
		</form> *@
		</div>
	</div>
</div>
